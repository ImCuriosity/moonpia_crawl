# 이 파일을 .env 로 복사한 뒤 실제 값을 채우세요.
#   copy .env.example .env      (Windows)
#   cp .env.example .env        (macOS/Linux)
#
# .env 는 .gitignore 에 등록되어 있어 커밋되지 않습니다.
# 유료 회차 댓글을 수집할 때만 필요하며, 무료 회차는 로그인 없이 수집됩니다.

MUNPIA_ID=
MUNPIA_PW=
