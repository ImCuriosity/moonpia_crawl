# 필수
requests>=2.31
pandas>=1.3

# 이탈 추론 (`train` 명령). 수집·전처리만 할 거면 없어도 된다.
scikit-learn>=1.1
joblib>=1.1

# 댓글 감정 분석 (`sentiment` 명령, KOTE). 없으면 감정 피처 없이 학습한다.
#   pip install torch transformers
#   CPU만 쓸 거면: pip install torch --index-url https://download.pytorch.org/whl/cpu

# 선택 — 브라우저 로그인이 필요할 때만 (캡차·SNS 로그인·2단계 인증)
#   pip install playwright
#   python -m playwright install chromium
#
# `playwright install`이 PATH에 없다는 오류가 나면 위처럼 `python -m` 을 붙인다.
# 패키지만 깔고 브라우저 바이너리를 안 받으면 창이 뜨지 않는다 — 두 줄 다 필요하다.
